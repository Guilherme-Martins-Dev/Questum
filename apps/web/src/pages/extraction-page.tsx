export function ExtractionPage() {
  return (
    <div className="mx-auto max-w-2xl p-8">
      <h1 className="text-2xl font-semibold">Configurar extração</h1>
      {/* TODO: ExtractionForm da feature "extraction" (disciplina etc.) */}
    </div>
  );
}
